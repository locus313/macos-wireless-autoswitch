## Description

<!-- What does this PR do and why? -->

## Changes

<!-- List the key changes made -->

- 

## How to Test

<!-- Steps to manually verify the changes work correctly on macOS -->

1. 
2. 

## Checklist

- [ ] `shellcheck wireless.sh install.sh` passes with no errors
- [ ] `xmllint --noout com.computernetworkbasics.wifionoff.plist` passes (if plist changed)
- [ ] Tested manually on macOS with a wired connection (if logic changed)
- [ ] `SUPPORTED_ADAPTERS` / `SUPPORTED_OS_VERSIONS` updated if adding new support
- [ ] README updated if user-facing behavior changed
- [ ] CHANGELOG.md updated
